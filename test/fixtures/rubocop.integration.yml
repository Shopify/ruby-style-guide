inherit_from:
  - ../../rubocop.yml

AllCops:
  SuggestExtensions: false # Reduce noise in test output

Style/FrozenStringLiteralComment:
  Enabled: false # We know this cop works, and having it enabled adds noise to test snippets
